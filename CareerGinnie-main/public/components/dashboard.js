document.addEventListener("DOMContentLoaded", function () {
  const dashboard = document.getElementById("dashboard-container");
  dashboard.innerHTML = `
        <section class="pt-24 pb-16 md:pb-24 relative overflow-hidden">
            <!-- Dashboard content -->
        </section>
    `;
});