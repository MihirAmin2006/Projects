document.addEventListener('DOMContentLoaded', function() {
    const jobMatching = document.getElementById('job-matching-container');
    jobMatching.innerHTML = `
        <section class="py-16 bg-white">
            <!-- Job Matching content -->
        </section>
    `;
});